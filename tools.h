#pragma once
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <string.h>
using namespace std;

void split(const string& s, vector<string>& sv, const char flag);
unsigned int Hash(const string& str);